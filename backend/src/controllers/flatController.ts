import { Request, Response, NextFunction } from "express";
import { PrismaClient } from "@prisma/client";

export const isInFlat = () => {
      /**
       * @DeVu999 TODO
       * sprawdzić czy User ma true czy false przy atrybucie isInFlat
       * Jesli tak to przenosisz do dashboardu
       * Jak nie to na screen z "Create" lub "Join" (the flat) :)
       */
}

export const createFlat = () => {
      /**
       * @SculptTechProject TODO
       * Stworzenie pokoju
       * Stworzenie autoryzacyjnego kodu
       */
}

export const joinFlat = () => {
      /**
       * @SculptTechProject TODO
       * Dołączenie do pokoju za pomocą unikatowego kodu
       */
}