import { Router } from "express";

const router = Router();

router.get("/flat, "); //check czy jest juz uzytkownik dodany do mieszkania
router.post("/create"); // tworzenie mieszkania
router.post("/join"); // dołączania do mieszkania

export default router;
